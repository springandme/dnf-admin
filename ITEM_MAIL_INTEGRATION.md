# 物品管理页面邮件发送功能集成说明

## 功能概述

在物品管理页面集成了邮件发送功能，用户可以直接从物品列表快速发送邮件，无需跳转到邮件管理页面。

## 功能特性

### 1. 多种操作方式
- **行内按钮**：每行物品都有"发送"按钮，支持单个物品快速发送
- **批量操作**：选择多个物品后，可批量发送邮件
- **详情页发送**：在物品详情抽屉中也可以发送邮件

### 2. 智能预填
- **物品信息**：自动预填选中的物品信息
- **物品类型**：智能映射物品类型到邮件系统类型
- **邮件内容**：根据物品信息生成默认邮件内容
- **邮件标题**：自动生成包含物品信息的标题

### 3. 用户体验优化
- **权限控制**：基于用户权限显示/隐藏功能
- **操作反馈**：发送成功后显示确认信息
- **错误处理**：友好的错误提示和处理
- **界面一致性**：与现有设计风格保持统一

## 使用方法

### 1. 单个物品发送
1. 在物品列表中找到目标物品
2. 点击该行的"发送"按钮
3. 在弹出的邮件表单中：
   - 选择收件角色
   - 确认邮件标题和内容
   - 设置附加金币（可选）
4. 点击"确定"发送邮件

### 2. 批量物品发送
1. 勾选要发送的多个物品
2. 点击底部工具栏的"批量发送邮件"按钮
3. 在邮件表单中确认信息并发送

### 3. 详情页发送
1. 点击物品行的"详情"按钮
2. 在详情抽屉右上角点击"发送邮件"按钮
3. 完成邮件发送流程

## 技术实现

### 1. 组件架构
```
物品管理页面 (ItemList)
├── 邮件发送组件 (MailSender)
├── 物品类型映射工具 (itemTypeMapping)
└── 权限控制 (Access)
```

### 2. 核心组件

#### MailSender 组件
- 位置：`antd/src/components/MailSender/index.tsx`
- 功能：通用邮件发送表单组件
- 特性：支持预填物品信息、智能生成邮件内容

#### 物品类型映射
- 位置：`antd/src/utils/itemTypeMapping.ts`
- 功能：将物品类型字符串映射为邮件系统数字类型
- 支持：精确匹配和模糊匹配

### 3. 状态管理
```typescript
// 邮件发送相关状态
const [mailSenderVisible, setMailSenderVisible] = useState<boolean>(false);
const [mailTargetItems, setMailTargetItems] = useState<API.DaItemEntity[]>([]);
```

### 4. 权限控制
所有邮件发送功能都基于 `access.hashPre('mail.sendMail')` 权限控制。

## 物品类型映射

### 支持的类型映射
| 物品类型字符串 | 邮件系统类型 | 类型值 |
|---------------|-------------|--------|
| 装备/武器/防具 | 装备 | 1 |
| 消耗品/药水 | 消耗品 | 2 |
| 材料/制作材料 | 材料 | 3 |
| 任务材料 | 任务材料 | 4 |
| 宠物 | 宠物 | 5 |
| 宠物装备 | 宠物装备 | 6 |
| 宠物消耗品 | 宠物消耗品 | 7 |
| 时装/外观 | 时装 | 8 |
| 副职业 | 副职业 | 10 |

### 映射逻辑
1. **精确匹配**：优先进行精确字符串匹配
2. **模糊匹配**：支持包含关系的模糊匹配
3. **默认类型**：无法匹配时默认为装备类型（1）

## 界面设计

### 1. 操作按钮布局
- **表格操作列**：详情 | 发送 | 删除
- **按钮样式**：链接按钮 + 图标 + Tooltip
- **权限控制**：基于用户权限动态显示

### 2. 批量操作工具栏
- **位置**：页面底部，选择物品时显示
- **按钮**：批量发送邮件 | 批量删除
- **样式**：主要按钮（发送）+ 危险按钮（删除）

### 3. 邮件发送表单
- **标题**：动态显示物品信息
- **预填内容**：智能生成邮件内容
- **物品展示**：只读展示选中的物品信息

## 错误处理

### 1. 权限检查
- 无邮件发送权限时隐藏相关按钮
- 权限不足时显示友好提示

### 2. 数据验证
- 必填字段验证
- 物品类型有效性检查
- 角色选择验证

### 3. 网络错误
- 发送失败时显示错误信息
- 支持重试机制
- 保持表单数据不丢失

## 性能优化

### 1. 组件复用
- MailSender 组件可在多个页面复用
- 统一的邮件发送逻辑和样式

### 2. 状态管理
- 最小化状态更新
- 合理的组件重渲染控制

### 3. 网络请求
- 复用现有的邮件发送API
- 合理的错误处理和加载状态

## 测试建议

### 1. 功能测试
- [ ] 单个物品发送邮件
- [ ] 批量物品发送邮件
- [ ] 详情页发送邮件
- [ ] 权限控制验证
- [ ] 物品类型映射验证

### 2. 用户体验测试
- [ ] 操作流程是否流畅
- [ ] 错误提示是否友好
- [ ] 界面布局是否合理
- [ ] 响应速度是否满意

### 3. 边界情况测试
- [ ] 无权限用户访问
- [ ] 网络异常情况
- [ ] 大量物品批量发送
- [ ] 特殊字符物品名称

## 扩展功能

### 1. 可选优化
- **模板邮件**：预设常用邮件模板
- **发送历史**：记录发送历史和统计
- **定时发送**：支持定时发送功能
- **收件人群组**：支持群组发送

### 2. 集成建议
- 可在其他物品相关页面集成类似功能
- 统一的邮件发送体验
- 考虑与游戏内邮件系统的深度集成

## 总结

通过在物品管理页面集成邮件发送功能，实现了：

- ✅ 操作路径最短化（减少页面跳转）
- ✅ 表单预填最大化（减少重复输入）
- ✅ 反馈及时性（操作结果即时显示）
- ✅ 错误处理友好性（清晰的错误提示）
- ✅ 界面一致性（与现有设计风格统一）

该功能显著提升了管理员的工作效率，提供了更好的用户体验。
