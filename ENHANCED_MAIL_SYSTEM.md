# DNF游戏管理系统 - 增强邮件发送功能

## 功能概述

本次更新为DNF游戏管理系统添加了强大的增强邮件发送功能，支持物品属性配置、批量发送、进度监控等高级特性。

## 🚀 新增功能

### 1. 物品属性配置
- **发送数量**：支持1-9999的数量输入
- **强化等级**：支持0-30级强化等级选择
- **锻造等级**：支持锻造等级设置
- **增幅配置**：支持增幅类型（无/体力/精神/力量/智力）和数值设置
- **封装状态**：提供是否封装的开关选项
- **金币数量**：支持同时发送金币

### 2. 红字属性设置
- **力量红字**：数值输入框
- **智力红字**：数值输入框  
- **精神红字**：数值输入框
- **体力红字**：数值输入框

### 3. 批量发送功能
- **多角色选择**：支持选择多个玩家进行批量发送
- **CSV文件导入**：支持导入玩家列表
- **文本输入**：支持直接输入角色信息
- **发送进度显示**：实时显示批量发送进度
- **结果反馈**：详细的成功/失败统计和错误信息

### 4. 模板管理
- **配置保存**：常用邮件配置保存为模板
- **快速复用**：一键应用已保存的模板

## 📱 用户界面

### 邮件管理页面
访问路径：`/mail`

页面新增了以下按钮：
- **普通邮件**：原有的简单邮件发送功能
- **增强邮件**：新的增强邮件发送功能
- **批量发送**：打开角色列表导入功能
- **清空全服邮件**：原有的清空邮件功能

### 增强邮件发送弹窗
- **发送类型选择**：单个发送 / 批量发送
- **收件人配置**：单个角色选择 / 多角色选择
- **邮件内容**：标题、正文、金币配置
- **物品配置表格**：支持添加多个物品并配置属性
- **模板设置**：保存为模板选项

### 角色列表导入弹窗
- **CSV文件上传**：支持CSV格式文件导入
- **文本输入**：支持直接输入角色信息
- **数据验证**：自动验证和去重
- **模板下载**：提供CSV模板下载

## 🛠️ 技术实现

### 后端增强
1. **数据结构扩展**
   - `MailItemDto`：新增物品属性字段
   - `SendMailDto`：新增批量发送字段
   - `BatchSendResultDto`：批量发送结果
   - `DaMailTemplate`：邮件模板实体

2. **服务层增强**
   - `GameMailService`：新增批量发送接口
   - `GamePostalService`：新增增强邮件发送方法
   - 支持同步和异步批量发送

3. **API接口**
   - `POST /api/mail/batchSendMail`：批量发送邮件
   - `POST /api/mail/asyncBatchSendMail`：异步批量发送
   - `GET /api/mail/batchSendProgress/{taskId}`：查询发送进度
   - `POST /api/mail/validateCharacList`：验证角色列表

### 前端组件
1. **EnhancedMailSender**：增强邮件发送组件
2. **PlayerListImporter**：角色列表导入组件
3. **ItemSelector**：物品选择器组件

## 📋 使用指南

### 1. 单个邮件发送
1. 点击"增强邮件"按钮
2. 选择"单个发送"
3. 选择收件角色
4. 配置邮件内容
5. 添加物品并设置属性
6. 点击发送

### 2. 批量邮件发送
1. 点击"批量发送"按钮导入角色列表
2. 上传CSV文件或直接输入角色信息
3. 确认导入后自动打开增强邮件发送
4. 配置邮件内容和物品属性
5. 点击发送并查看进度

### 3. CSV文件格式
```csv
角色编号,角色名称
1001,示例角色1
1002,示例角色2
1003,示例角色3
```

### 4. 文本输入格式
```
1001 示例角色1
1002 示例角色2
1003 示例角色3
```

## 🔧 配置说明

### 数据库配置
系统会自动创建 `da_mail_template` 表用于存储邮件模板。

### 权限配置
所有邮件发送功能都基于 `mail.sendMail` 权限控制。

## 🚨 注意事项

1. **大批量发送**：超过10个角色的批量发送会自动切换为异步模式
2. **进度监控**：异步发送时会显示实时进度弹窗
3. **错误处理**：发送失败时会显示详细的错误信息
4. **数据验证**：系统会自动验证角色编号格式和去重

## 🔄 兼容性

- 完全兼容现有的邮件发送功能
- 保留原有的简单邮件发送接口
- 新功能为可选使用，不影响现有工作流程

## 📞 技术支持

如有问题或建议，请联系开发团队。
